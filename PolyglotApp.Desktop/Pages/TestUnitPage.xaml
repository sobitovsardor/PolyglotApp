<Page x:Class="PolyglotApp.Desktop.Pages.TestUnitPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="TestUnitPage">

    <Page.Resources>
        <Style x:Key="UnitButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#9013FE"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Margin" Value="0,5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="10"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#6A0DAD"/>
                    <Setter Property="Effect">
                        <Setter.Value>
                            <DropShadowEffect Color="Black" BlurRadius="8" ShadowDepth="2" Opacity="0.4"/>
                        </Setter.Value>
                    </Setter>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Page.Resources>

    <Grid Background="White">

        <!-- 🔴 RESET BUTTON (ustida chiqishi uchun oxirida ZIndex=10 qo‘shamiz) -->
        <Button Content="⟳ Reset"
                Width="90" Height="35"
                HorizontalAlignment="Right"
                VerticalAlignment="Top"
                Margin="15"
                Background="#E74C3C"
                Foreground="White"
                FontWeight="Bold"
                BorderThickness="0"
                Cursor="Hand"
                Panel.ZIndex="10"
                Click="ResetSectionResults_Click"/>

        <!-- Scroll bilan unitlar -->
        <ScrollViewer VerticalScrollBarVisibility="Auto" Margin="0,60,0,0">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Top" Margin="20">
                <TextBlock Text="Select a Unit for Test"
                           FontSize="24"
                           FontWeight="Bold"
                           Margin="0,0,0,20"
                           TextAlignment="Center"
                           HorizontalAlignment="Center"/>

                <ItemsControl ItemsSource="{Binding Units}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <Button Style="{StaticResource UnitButtonStyle}"
                                        Content="{Binding Title}"
                                        Tag="{Binding Title}"
                                        Width="200"
                                        Click="UnitButton_Click"/>
                                <TextBlock Text="{Binding BestResult}"
                                           FontSize="14"
                                           Foreground="#555"
                                           Margin="10,0,0,0"
                                           VerticalAlignment="Center"/>
                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
